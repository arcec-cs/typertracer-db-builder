//imports
const FS = require("fs");
const PATH = require("path");

//static data needed in multiple files process to obj once then export(this works and is efficent because Module are cached after they are loaded) 
const PG_META_INDEX = JSON.parse(FS.readFileSync(PATH.resolve(__dirname,".//pg-data//pg-metadata//pg-metadata.json")));//generated by hugovk/gutenberg-metadata
const FILE_PATH_INDEX = JSON.parse(FS.readFileSync(PATH.resolve(__dirname,".//pre-processor-generated//textFilePathIndex.json"))); //keys: ids values: filepaths, generated from traverse.js 
const IDS_INPUT = FS.readFileSync(PATH.resolve(__dirname,".//ids-csv//ids.csv"), "utf-8").split(","); //contains 415 texts of the top 450 excluding repeats; 2010 contains 290

//exports
module.exports ={
  PG_META_INDEX: PG_META_INDEX,
  FILE_PATH_INDEX: FILE_PATH_INDEX,
  IDS_INPUT: IDS_INPUT
};